import Body

class Spring:
    def __init__(self, body1, body2, length, constant):
        self.point1 = point1
        self.point2 = point2
        self.length = float(length)
        self.constant = (constant)



b = (1,3)
a = Spring(b, b, 5, 20)
